# Database Recovery SOP

If database latency rises above 500ms, activate a replica or divert read traffic away from the primary instance. Reduce retry pressure and validate transaction consistency before resuming background jobs.
